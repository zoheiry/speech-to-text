const defaultValues = {
  bodyTopPadding: '54px',
  defaultShadow: 'box-shadow: 0 1px 4px rgba(0, 0, 0, 0.2)',
};

const colors = {
  primary: '#0cb67e',
  danger: '#ec4c21',
  success: '#219fec',
  warning: '#eccc21',
  textColor: '#1b2b3b',
}

const theme = {
  ...colors,
  ...defaultValues
};

export default theme;
